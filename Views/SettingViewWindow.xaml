<Window x:Class="TamaPoke.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewmodels="clr-namespace:TamaPoke.ViewModels"
        Title="게임 설정" Height="420" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Window.DataContext>
        <viewmodels:SettingsViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. 트레이 알림 설정 -->
        <CheckBox Grid.Row="0" Content="시스템 트레이 알림 사용" 
                  IsChecked="{Binding UseTrayNotifications}" 
                  VerticalContentAlignment="Center" Margin="0,0,0,15"/>

        <!-- 2. 부화 세대 안내 텍스트 -->
        <TextBlock Grid.Row="1" Text="부화 대상 세대 선택 (지방별)" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- 3. 세대별 체크박스 목록 (ItemsControl) -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Generations}" Margin="0,0,0,15">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding DisplayText}" 
                              IsChecked="{Binding IsSelected}" 
                              Margin="5,4" VerticalContentAlignment="Center"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 4. 저장 버튼 -->
        <Button Grid.Row="3" Content="저장 및 닫기" 
                Command="{Binding SaveCommand}" 
                Width="110" Height="32" HorizontalAlignment="Right"/>
    </Grid>
</Window>